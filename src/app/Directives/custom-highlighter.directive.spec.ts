import { CustomHighlighterDirective } from './custom-highlighter.directive';

describe('CustomHighlighterDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomHighlighterDirective();
    expect(directive).toBeTruthy();
  });
});
