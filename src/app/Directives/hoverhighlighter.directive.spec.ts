import { HoverhighlighterDirective } from './hoverhighlighter.directive';

describe('HoverhighlighterDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverhighlighterDirective();
    expect(directive).toBeTruthy();
  });
});
